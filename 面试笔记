禅道和qc区别和用途
禅道和 QC 在功能和用途上有很大的区别。禅道更侧重于项目管理和团队协作，而 QC 更侧重于软件质量管理和测试管理。您可以根据团队的需求和项目的特点选择适合的工具。

什么是lambda函数？请举例说明？
Lambda函数是一种匿名函数，也称为内联函数或者无名函数。它是一种在需要时声明的小型函数，通常用于在代码中传递简单的功能而不必定义完整的函数。
下面是一个简单的例子，演示了如何使用lambda函数来计算两个数字的和：
addition = lambda x, y: x + y
result = addition(3, 5)
print(result)  # 输出结果为8
在这个例子中，lambda函数接受两个参数x和y，并返回它们的和。然后我们调用这个lambda函数并传入参数3和5，得到结果8。Lambda函数通常用于需要传递函数作为参数的场景，或者在需要简洁地定义某些功能时使用。


在Python中，可以使用list()函数将元组（tuple）转换为列表（list），也可以使用tuple()函数将列表转换为元组。下面分别给出示例代码：
将元组转换为列表的示例：
python
my_tuple = (1, 2, 3)
my_list = list(my_tuple)
print(my_list)  # 输出结果为 [1, 2, 3]
将列表转换为元组的示例：

python
my_list = [4, 5, 6]
my_tuple = tuple(my_list)
print(my_tuple)  # 输出结果为 (4, 5, 6)
通过调用list()函数并传入一个元组，我们将元组转换为了一个列表。同样地，通过调用tuple()函数并传入一个列表，我们将列表转换为了一个元组。这两个函数都能够快速方便地进行类型转换。

在Python中，match()和search()是正则表达式模块re中用于匹配字符串的两个函数，它们之间有以下区别：

match():
match()函数从字符串的开头开始匹配，只有当字符串的开头就满足匹配条件时才会返回匹配对象，否则返回None。
也就是说，match()函数只会尝试匹配目标字符串的开头部分，如果开头不匹配，则不会继续往后匹配。
search():

search()函数将在整个字符串中搜索匹配项，一旦找到符合条件的匹配项就会返回匹配对象，否则返回None。
与match()不同，search()函数会尝试在整个字符串中查找匹配项，而不局限于字符串的开头。
下面是两个函数的简单示例代码：

python
import re
text = "The cat is on the mat"

# 使用match()函数
result_match = re.match(r'cat', text)
print(result_match)  # 返回None，因为'cat'不在开头

# 使用search()函数
result_search = re.search(r'cat', text)
print(result_search.group())  # 返回'cat'
在上述示例中，由于"cat"并不出现在字符串的开头，所以match()函数返回了None；而search()函数在整个字符串中找到了"cat"，并成功返回了匹配对象。

线程（Thread）和进程（Process）是实现并发编程的两种方式
1.线程是进程的一个执行单元，在进程中，各个线程都是资源共享的
2.进程是操作系统分配资源的基本单位，可独立运行的程序
资源部分：
1.线程在同一进程内共享相同的内存空间和文件描述符等资源。多个线程之间通过共享内存来进行通信和数据交换
2.进程拥有独立的内存空间和文件描述符等资源。不同进程之间通常使用进程间通信（IPC）机制进行数据交换。（共享内存、消息队列、管道和是sockect套接字）
并发能力：
1.线程开销能力小，创建线程速度和销毁速度快，建立多线程提高并发能力
2.线程开销能力慢，创建线程速度和销毁速度慢，不同进程之间相互独立，各自拥有独立的资源，所以多个进程可以并发执行，但进程间的切换需要较大的开销。

如何保证线程的安全：
1.互斥锁：条件变量当一个线程获得锁时，其他线程将被阻塞，直到该线程释放锁。
2.条件变量：可以在某种条件满足时唤醒等待的线程
3.使用诸如读写锁、屏障、条件变量等同步工具来协调线程的执行顺序。
4.使用安全的数据结构

Python 字典参数如何传递？
1.位置参数，将字典拆解，以××的方式进行传递
2.关键字参数，将参数以关键字的形式进行传递，以××的方式进行传递

闭包：
1.闭包是指函数可以访问其词法作用域外部的变量
2.工厂函数
3.回调函数
3.装饰器

在Python中，list，tuple，dict，set 有什么区别，主要应用在什么场景？
1.列表：list适合用于有序、可变的元素集合
2.元组：tuple适合用于不可变的元素集合
3.字典：dict适合用于键值对映射
4.集合：set适合用于存储无序且唯一的元素集合

阐述Python变量作用域？
1.局部作用域
2.全局作用域
3.嵌套作用域
4.内置作用域

请阐述*args and **kwargs 的区别？
1.*args传进去的是类似元组
2.**kwargs传进去的形式类似与字典

